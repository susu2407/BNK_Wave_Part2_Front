name: bnkpart2
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.5.0
  flutter_secure_storage: ^9.2.4
  provider: ^6.1.5
  cupertino_icons: ^1.0.8 # 버전 수정

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0 # 버전 수정

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/terms/
